export const config = {
    // Server
    api_host: 'localhost',
    api_port: 5000,

    // Database
    db_name: 'chillout',
    db_host: 'localhost',
    db_port: 8432,
    db_user: '...',
    db_password: '...',

    // Remote api url's
};